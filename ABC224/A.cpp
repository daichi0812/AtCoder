#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for(int i = 0; i < (n); ++i)

int main(){
    string s;
    cin >> s;

    int end = s.size();

    if(s.at(end - 2) = 'e' && s.at(end - 1) == 'r'){
        cout << "er" << endl;
    }else {
        cout << "ist" << endl;
    }

}